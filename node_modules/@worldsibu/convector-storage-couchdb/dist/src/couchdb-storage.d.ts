import { BaseStorage } from '@worldsibu/convector-core';
export declare class CouchDBStorage extends BaseStorage {
    defaultDB: string;
    private couch;
    constructor(config: any, defaultDB?: string);
    updateDefaultDB(defaultDB: string): void;
    query(...args: any[]): Promise<any[]>;
    get(id: string): Promise<any>;
    set(id: string, content: any): Promise<void>;
    delete(id: string): Promise<void>;
    history(id: string): Promise<any[]>;
}
