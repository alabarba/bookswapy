import { ClientConfig, ClientHelper } from '@worldsibu/convector-common-fabric-helper';
import { IConfig as ControllersConfig, Config, KV } from '@worldsibu/convector-core-chaincode';
export { ControllersConfig };
export interface ManagerConfig extends ClientConfig {
    policy?: any;
    npmrc?: string;
    npmtoken?: string;
    controllers: ControllersConfig[];
}
export declare class Manager extends ClientHelper {
    config: ManagerConfig;
    static fromConfig(path: string): Manager;
    static readConfig(path: string): ManagerConfig;
    static extractError(err: string): {
        status: number;
        message: string;
    } | void;
    static getCCName(packageName: string): string;
    chaincodeConfig: Config;
    constructor(config: ManagerConfig);
    init(initKeyStore?: boolean): Promise<void>;
    install(name: string, version: string, path: string): Promise<void>;
    instantiate(name: string, version: string, ...args: string[]): Promise<void>;
    upgrade(name: string, version: string, ...args: string[]): Promise<void>;
    initControllers(name: string, adminOrUser?: string | true): Promise<void>;
    package(output: string, controllers?: KV, update?: boolean): Promise<void>;
}
