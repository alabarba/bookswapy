import { BaseGenerator } from './base';
export declare class NetworkRestartShOptions {
    networkRootPath: string;
    users: number;
    channels: string[];
    organizations: string[];
    insideDocker: boolean;
    envVars: {
        FABRIC_VERSION: string;
        THIRDPARTY_VERSION: string;
    };
}
export declare class NetworkRestartShGenerator extends BaseGenerator {
    private options;
    success: string;
    contents: string;
    constructor(filename: string, path: string, options: NetworkRestartShOptions);
}
