import { BaseGenerator } from './base';
export declare class InstallChaincodeShOptions {
    networkRootPath: string;
    orgs: string[];
    channel: string;
    name: string;
    colConfig?: string;
    version: string;
    language: string;
    currentPath: string;
    params: string;
    hyperledgerVersion: string;
    insideDocker: boolean;
    debug: boolean;
}
export declare class InstallChaincodeShGenerator extends BaseGenerator {
    private options;
    success: string;
    contents: string;
    constructor(filename: string, path: string, options: InstallChaincodeShOptions);
    getPolicy(orgs: string[]): string;
}
