import { BaseGenerator } from './base';
export declare class CryptoGeneratorOptions {
    networkRootPath: string;
    orgs: string[];
    channels: string[];
    envVars: {
        FABRIC_VERSION: string;
    };
}
export declare class CryptoGeneratorShGenerator extends BaseGenerator {
    private options;
    success: string;
    contents: string;
    constructor(filename: string, path: string, options: CryptoGeneratorOptions);
}
