import { BaseGenerator } from './base';
export declare class DockerComposeYamlOptions {
    networkRootPath: string;
    orgs: string[];
    envVars: {
        FABRIC_VERSION: string;
        THIRDPARTY_VERSION: string;
    };
}
export declare class DockerComposeYamlGenerator extends BaseGenerator {
    private options;
    success: string;
    constructor(filename: string, path: string, options: DockerComposeYamlOptions);
    build(): Promise<void>;
    discoverCert(org: string): Promise<string>;
}
