import { BaseGenerator } from './base';
export declare class NetworkCleanShOptions {
    removeImages: boolean;
}
export declare class NetworkCleanShGenerator extends BaseGenerator {
    private options;
    success: string;
    contents: string;
    constructor(filename: string, path: string, options: NetworkCleanShOptions);
}
