import { BaseGenerator } from './base';
export declare class NetworkProfileYamlGeneratorOptions {
    org: string;
    networkRootPath: string;
    channels: string[];
    orgs: string[];
    insideDocker: boolean;
}
export declare class NetworkProfileYamlGenerator extends BaseGenerator {
    private options;
    contents: string;
    constructor(filename: string, path: string, options: NetworkProfileYamlGeneratorOptions);
}
