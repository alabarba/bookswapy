import * as Insight from 'insight';
export declare class Analytics {
    trackingCode: string;
    insight: Insight;
    constructor();
    init(): Promise<void>;
    trackNetworkNew(label?: string): void;
    trackNetworkClean(label?: string): void;
    trackChaincodeInstall(label?: string): void;
    trackChaincodeUpgrade(label?: string): void;
    trackChaincodeInvoke(label?: string): void;
    track(category: CategoryEnum, action: ActionEnum, label?: string): void;
    permissionAcceptance(): void;
    permissionRejection(): void;
    returnAccepted(): void;
    returnRejected(): void;
}
export declare enum CategoryEnum {
    NETWORK = "network",
    CHAINCODE = "chaincode",
    TRACKING = "tracking"
}
export declare enum ActionEnum {
    NEW = "new",
    CLEAN = "clean",
    INSTALL = "install",
    UPGRADE = "upgrade",
    INVOKE = "invoke"
}
