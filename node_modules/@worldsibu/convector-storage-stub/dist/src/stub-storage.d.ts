import { ChaincodeStub } from 'fabric-shim';
import { BaseStorage } from '@worldsibu/convector-core';
import { StubHelper } from '@theledger/fabric-chaincode-utils';
export declare class StubStorage extends BaseStorage {
    stubHelper: StubHelper;
    constructor(stub: ChaincodeStub);
    query(query: any): Promise<any[]>;
    get(id: string, storageOptions?: any): Promise<any>;
    set(id: string, content: any, storageOptions?: any): Promise<any>;
    delete(id: string, storageOptions?: any): Promise<void>;
    history(id: string): Promise<any[]>;
}
